# Level 0 静态势阱自洽检查汇总报告

## 一、范围与边界

- `x` 为高斯光束焦平面径向坐标，**不**是光轴方向；不模拟轴向 5.64 kHz 运动。
- 仅静态、一维、经典、单原子；不含 SLM-AOD 交接、深度时变、运输、二维、噪声、
  量子内态、加热、损失或多原子。
- SLM 与 AOD 调用同一高斯势实现，仅由配置参数与来源区分。
- 数值曲率 vs 解析曲率为**代码一致性检查**，非独立实验验证。
- 默认 SLM(180 μK) 与 AOD(280 μK) 为两个独立静态势阱的 Level 0 比较，
  不代表论文 Fig.6 任意时刻的实际重叠势（该处 SLM 约 140 μK）。

## 二、参数

- 原子：Cs-133，质量 132.905 u = 2.20695e-25 kg。
- 网格：4001 点，偏移 [-4, 4] w。
- 轨迹：初始位移 0.1 w，初速 0 m/s，时长 3000 μs，dt 0.05 μs。

## 三、主要结果

| 势阱 | 深度(μK) | 束腰(μm,来源) | f_解析(kHz) | f_数值曲率(kHz) | f_非线性(kHz) | f_轨迹(kHz) | 离散化误差 | 非简谐频移 | max|ΔE|/U0 |
|---|---|---|---|---|---|---|---|---|---|
| SLM | 180 | 1.17 | 28.8700 | 28.8699 | 28.6516 | 28.6544 | 5.00e-07 | 7.562e-03 | 4.03e-07 |
| AOD | 280 | 1.17(assumed) | 36.0072 | 36.0072 | 35.7349 | 35.7384 | 5.00e-07 | 7.562e-03 | 6.27e-07 |

## 四、误差分类说明

- **数值离散化误差**：数值曲率频率 vs 解析小振幅频率（目标 < 1e-4）。
- **有限振幅非简谐频移**：小振幅频率 vs 非线性积分频率（约 1–3%，物理软化，非积分误差）。
- **轨迹积分误差**：轨迹频率 vs 同能量非线性积分频率（严格数值容差）。

## 五、验收状态

- **SLM**：曲率容差=True，谐振频率容差=True，轨迹频率容差(3%)=True，能量误差容差=True，束缚=True，周期数充足=True。
- **AOD**：曲率容差=True，谐振频率容差=True，轨迹频率容差(3%)=True，能量误差容差=True，束缚=True，周期数充足=True。

全部验收项通过：True

## 六、图像检查

- 程序化图像检查：7/7 张通过（文件大小、Pillow 重开、像素宽高、方差、有限性、
  图例/系列数与数据范围一致）。
- **已执行真正视觉审阅**（`visual_review_performed: true`）：7 张 PNG 全部打开；
  其中 5 张（势/力比较、SLM/AOD 能量、频率比较）经图像视觉模型描述核验，
  2 张轨迹图经 Read 工具内联图像渲染直接查看。
- 审阅发现：所有图物理正确（高斯阱、奇对称力、有界近正弦轨迹、微小有界能量误差、
  符合预期的非简谐频率排序），英文标签清晰，无文字重叠或裁切，无缺陷。
- 详细逐张发现见 `image_validation.json` 中每张图的 `visual_review_finding` 字段。

## 七、尚未覆盖的物理

- 论文轴向 5.64 kHz 运动、SLM-AOD 交接、时变深度、长距离运输、
  AOD 柱面透镜效应、二维运动、噪声、量子内态演化、加热、损失、多原子。
