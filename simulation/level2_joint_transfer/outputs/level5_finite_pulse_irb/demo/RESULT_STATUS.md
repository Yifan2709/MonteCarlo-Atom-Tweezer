# Level 5 历史结果状态（2026-09-14）

此目录保存修复前的历史结果。原始 JSON、CSV 与图片保留用于追溯，没有用修复后的代码冒名替换其来源。

| 结果 | 状态 |
|---|---|
| 单通道 PTM / channel_metrics.json | 历史输出；不经过 IRB 索引路径。目标通常为恒等门，不能把 spin-echo 相对恒等门的约 1/3 解释为完全失相干。 |
| reference RB | 历史输出；独立路径，单指数并非旧输出的优选模型。 |
| interleaved RB、47/195 阵列统计及相关图片 | 受移动索引错误影响，必须重跑，不作有效定量结论。 |
| 标准 IRB F_avg=1.001650 | 不可解释，且固定 N、扫描 M 的设计不能直接套用不同自变量的 p_int/p_ref。 |

本轮修复：

1. 多站点移动索引改为 `sequence_index * n_sites + site_index`，避免污染其他序列并遗漏站点。
2. 每次 move 之后累计真实移动时长，使后续门读取正确的回放时间。
3. 固定 N 的 M 扫描保留衰减拟合，禁止发布标准 IRB 比值保真度；一般估计函数也拒绝非有限、越界参数及 F>1。
4. 增加 1/3/47 站点、M=0/1/2/60 的解析回归。

仍有限制：每次 move 独立抽取单轮轨迹，缺少连续多轮机械加热记忆；默认噪声和站点散布关闭；transformed Clifford frame 未接入正式传播；经典 survival 不因站点阱深散布而重采样。

完整重跑应使用新输出目录，并记录配置、源代码和上游轨迹的 SHA-256。
